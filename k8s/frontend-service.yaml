apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  namespace: ai-website-builder
  labels:
    app: frontend
spec:
  type: ClusterIP
  selector:
    app: frontend
  ports:
  - name: http
    port: 80
    targetPort: 80
    protocol: TCP
  sessionAffinity: None
